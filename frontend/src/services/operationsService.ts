const base=import.meta.env.VITE_API_BASE_URL||'/api';const request=async<T>(path:string,init?:RequestInit):Promise<T>=>{const token=sessionStorage.getItem('eahts-admin-token');const res=await fetch(`${base}${path}`,{...init,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{}) ,...(init?.headers??{})}});if(!res.ok){const body=await res.json().catch(()=>({}));throw new Error(body.message??'Request failed.')}return res.json() as Promise<T>};
export type VolunteerRecord={id:string;name:string;role:string;shift:string;contact:string;status:string};export type Venue={id:string;name:string;location?:string;capacity:number;status:string;notes?:string};export type Equipment={id:string;name:string;quantity:number;status:string;assigned_to?:string};export type Task={id:string;title:string;description?:string;priority:string;assigned_to?:string;status:string;due_date?:string};export type Incident={id:string;title:string;description?:string;severity:string;status:string;assigned_to?:string};
export const getOperationsReadiness=()=>request<{venue:number;equipment:number;volunteers:number;tasksComplete:number;openIncidents:number;readiness:number}>('/admin/operations/readiness');export const listVolunteers=async()=> (await request<{volunteers:VolunteerRecord[]}>('/admin/operations/volunteers')).volunteers;export const updateVolunteer=(id:string,input:Partial<VolunteerRecord>)=>request<VolunteerRecord>(`/admin/operations/volunteers/${id}`,{method:'PATCH',body:JSON.stringify(input)});export const listVenues=async()=> (await request<{venues:Venue[]}>('/admin/operations/venues')).venues;export const createVenue=(input:Omit<Venue,'id'>)=>request<Venue>('/admin/operations/venues',{method:'POST',body:JSON.stringify(input)});export const listEquipment=async()=> (await request<{equipment:Equipment[]}>('/admin/operations/equipment')).equipment;export const listTasks=async()=> (await request<{tasks:Task[]}>('/admin/operations/tasks')).tasks;export const createTask=(input:Omit<Task,'id'>)=>request<Task>('/admin/operations/tasks',{method:'POST',body:JSON.stringify(input)});export const updateTask=(id:string,input:Partial<Task>)=>request<Task>(`/admin/operations/tasks/${id}`,{method:'PATCH',body:JSON.stringify(input)});export const listIncidents=async()=> (await request<{incidents:Incident[]}>('/admin/operations/incidents')).incidents;export const createIncident=(input:Omit<Incident,'id'>)=>request<Incident>('/admin/operations/incidents',{method:'POST',body:JSON.stringify(input)});
