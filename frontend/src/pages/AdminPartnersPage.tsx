import { useEffect, useState } from 'react';
import AuditTimeline from '../components/AuditTimeline'; import ConfirmModal from '../components/ConfirmModal'; import AdminWorkspaceLayout from '../layouts/AdminWorkspaceLayout'; import { usePopup } from '../contexts/PopupContext'; import { useProgress } from '../contexts/ProgressContext'; import { createPartner,deletePartner,getPartners,updatePartner,updatePartnerStatus,type PartnerInput,type PartnerRecord,type PartnerStatus } from '../services/partnerService';
const blank:PartnerInput={company:'',category:'',contactPerson:'',email:'',phone:'',country:'',status:'Pending',agreementStatus:'draft',logo:null};
export default function AdminPartnersPage(){const[partners,setPartners]=useState<PartnerRecord[]>([]);const[stats,setStats]=useState({total:0,confirmed:0,pending:0,partnerValue:0});const[form,setForm]=useState<PartnerInput>(blank);const[editing,setEditing]=useState<PartnerRecord|null>(null);const[formOpen,setFormOpen]=useState(false);const[confirm,setConfirm]=useState<{kind:'save'|'status'|'delete';partner?:PartnerRecord;status?:PartnerStatus}|null>(null);const[loading,setLoading]=useState(true);const{showPopup}=usePopup();const{openProgress,closeProgress}=useProgress();const load=async()=>{setLoading(true);try{const r=await getPartners();setPartners(r.partners);setStats(r.stats)}catch(e){showPopup({type:'error',message:e instanceof Error?e.message:'Unable to load partners.'})}finally{setLoading(false)}};useEffect(()=>{void load()},[]);const openNew=()=>{setEditing(null);setForm(blank);setFormOpen(true)};const openEdit=(p:PartnerRecord)=>{setEditing(p);setForm({...p});setFormOpen(true)};const submit=()=>{if(!form.company||!form.category||!form.contactPerson||!form.email){showPopup({type:'error',message:'Complete the required partner information.'});return}setConfirm({kind:'save',partner:editing??undefined})};const execute=async()=>{if(!confirm)return;const isDelete=confirm.kind==='delete';openProgress({title:isDelete?'Removing partner':confirm.kind==='status'?'Updating partner status':'Saving partner',description:'Updating the database and creating an audit record.',steps:[{label:'Saving changes',status:'active'}]});try{if(confirm.kind==='save'){editing?await updatePartner(editing.id,form):await createPartner(form);setFormOpen(false)}else if(confirm.kind==='status'&&confirm.partner&&confirm.status)await updatePartnerStatus(confirm.partner.id,confirm.status);else if(confirm.partner)await deletePartner(confirm.partner.id);await load();showPopup({type:'success',message:isDelete?'Partner removed successfully.':confirm.kind==='status'?'Partner status updated successfully.':'Partner saved successfully.'})}catch(e){showPopup({type:'error',message:e instanceof Error?e.message:'Unable to complete action.'})}finally{closeProgress();setConfirm(null)}};const change=(key:keyof PartnerInput,value:string)=>setForm(x=>({...x,[key]:value}));return <AdminWorkspaceLayout title="Partners" description="Manage partner records, agreements, and approvals."><section className="metric-grid finance-metrics">{[['Total partners',stats.total],['Confirmed',stats.confirmed],['Pending',stats.pending],['Partner value',stats.partnerValue]].map(([label,value])=><article className="metric-card" key={String(label)}><span>{label}</span><strong>{typeof value==='number'&&String(label)==='Partner value'?`$${value}`:value}</strong></article>)}</section><section className="command-workspace"><div className="workspace-heading"><div><p className="eyebrow">PARTNER DIRECTORY</p><h2>Partner roster</h2></div><button className="btn btn-primary" onClick={openNew}>Add partner</button></div>{loading?<div className="workspace-loading">Loading partner records</div>:<div className="finance-table-wrap"><table className="registrations-table"><thead><tr><th>Company</th><th>Category</th><th>Contact</th><th>Country</th><th>Status</th><th>Agreement</th><th>Actions</th></tr></thead><tbody>{partners.length?partners.map(p=><tr key={p.id}><td>{p.company}</td><td>{p.category}</td><td>{p.contactPerson}<br/><small>{p.email}</small></td><td>{p.country||'—'}</td><td>{p.status}</td><td>{p.agreementStatus}</td><td><button className="text-button" onClick={()=>openEdit(p)}>Edit</button>{p.status==='Pending'&&<><button className="text-button" onClick={()=>setConfirm({kind:'status',partner:p,status:'Confirmed'})}>Approve</button><button className="text-button" onClick={()=>setConfirm({kind:'status',partner:p,status:'Rejected'})}>Reject</button></>}<button className="text-button danger" onClick={()=>setConfirm({kind:'delete',partner:p})}>Delete</button></td></tr>):<tr><td colSpan={7}>No partner records yet.</td></tr>}</tbody></table></div>}</section><AuditTimeline/><ConfirmModal open={formOpen} title={editing?'Edit partner':'Add partner'} description="Enter the partner details before saving this record." confirmLabel="Review changes" onCancel={()=>setFormOpen(false)} onConfirm={submit}><div className="admin-form-grid"><input value={form.company} onChange={e=>change('company',e.target.value)} placeholder="Company *"/><input value={form.category} onChange={e=>change('category',e.target.value)} placeholder="Category *"/><input value={form.contactPerson} onChange={e=>change('contactPerson',e.target.value)} placeholder="Contact person *"/><input type="email" value={form.email} onChange={e=>change('email',e.target.value)} placeholder="Email *"/><input value={form.phone} onChange={e=>change('phone',e.target.value)} placeholder="Phone"/><input value={form.country} onChange={e=>change('country',e.target.value)} placeholder="Country"/><select value={form.agreementStatus} onChange={e=>change('agreementStatus',e.target.value)}><option value="draft">Agreement: draft</option><option value="sent">Agreement: sent</option><option value="signed">Agreement: signed</option><option value="expired">Agreement: expired</option></select></div></ConfirmModal><ConfirmModal open={Boolean(confirm)} title={confirm?.kind==='delete'?'Delete partner?':confirm?.kind==='status'?`${confirm.status} partner?`:'Save partner?'} description={confirm?.kind==='delete'?'This permanently removes the partner record.':confirm?.kind==='status'?'This changes the partner approval status.':'This creates or updates the partner record.'} confirmLabel={confirm?.kind==='delete'?'Delete':confirm?.kind==='status'?'Confirm':'Save'} destructive={confirm?.kind==='delete'} onCancel={()=>setConfirm(null)} onConfirm={()=>void execute()}/></AdminWorkspaceLayout>}
